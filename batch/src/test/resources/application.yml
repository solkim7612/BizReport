spring:
  datasource:
    url: jdbc:h2:mem:batch_test;MODE=MySQL
    driver-class-name: org.h2.Driver
    username: samadal
    password:
  batch:
    jdbc:
      initialize-schema: always
    job:
      enabled: false
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

api:
  nts:
    url: "http://localhost:8080/mock"
    key: "test-key"

dir:
  upload:
    rate: "src/test/resources/temp/rate/"
    card: "src/test/resources/temp/card/"
  download:
    card: "src/main/resources/static/"

shedlock:
  table-name: shedlock